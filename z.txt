教训：临时计算共用了返回值用 buf

这种头匹配，不妥当、依靠人力判断的地方，
定义的名字需要统筹，乱的危险极大，大可不必为兼容，试下其他想法，主要是整体识别，加尾？
变量的覆盖
生成参数栈（是能间接）

变量里存放是 reflect Value？
buf2___ 里面是 list，各项是 var___，这样到使用时才组合（如果按一开始就是使用来理解，那……）

定义甲以1。
显示甲。
甲甲甲甲。
定义没有识别

赋予1以2。
如果算术1+‘1’等于2那么显示1否则显示2。
逻辑运算符被算术收了

顿号还是仅在调用、赋予时用，这样如果就不必求值也用先，而先就可专用于逻辑嵌套

赋予接受多返回值，一般情况是相当于多赋值，调用、算术可以返回多值
错误陷阱，发生错误先找前面那条陷阱
有错误不活便不必算术返回两个值？


如果浏览器是操作系统，那它也需要一种脚本

智能设备的控制如果走服务器（http）这种方式那么就需要一种在服务器端运行的使灵活性最大的脚本

参数-1，前面的第一个参数，也是句号为界

这种方式定义怎么实现？
上级的变量区知道，本级的已申请变量不从区而是已有的代码段里搜。但那名字需要运算

循环的一干语句，循环最后一条，最前一条是标签，所有均执行

文件打开的结构，保存每次的所在目录，这样在打不开时加了试，或逐级，或.直转当前目录

对代码那段求 md5，这就是 cache 的 id。

删除无报错

注解 annota

先把代码分析成数组？
每个元素有类型
句号将被分析没，注释被去掉

文字
类型：文字。s：无引号、有引号合并到一起的一串，但到开单引号终止
去掉空格符

循环
类型：循环。codes：代码数组
遇到跳出直接执行下一句即可

先、了
横向的代码数组
